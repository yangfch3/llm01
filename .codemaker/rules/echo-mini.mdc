---
description: echo-mini 开发约定，Agent 编写 Echo/echo-mini/ 下代码时自动加载。
globs: 
  - "**/Echo/echo-mini/**"
---
<rule>

# echo-mini 开发约定

## 包导入

`Echo/echo-mini/src/echo_mini/` 不是 pip 安装的包。  
所有 `scripts/`、`tokenizer/` 下需要 `import echo_mini` 的脚本，必须在文件顶部（标准库 import 之后、第三方 import 之前）加入：

```python
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))
```

确保 `src/` 在 `sys.path` 中，再 `from echo_mini.xxx import ...`。

## 运行方式

- 项目依赖已 uv sync，不需要 `--extra` 参数
- 运行脚本统一用 `uv run python ...` 或 `uv run accelerate launch ...`
- 禁止直接调用 `.venv\Scripts\python.exe` 或裸 `python`

## 工作目录

训练相关命令的 cwd 为 `Echo/echo-mini/`，配置中的相对路径（如 `data/bin/train.bin`）基于此目录。

</rule>
